# Changelog

## 1.0.0 — 2026-08-20

### 修复

- 调整直播音频 VAD 的噪声底、前置缓冲、停顿收尾和队列调度，降低低音量语音漏识别和连续分段阻塞。
- 本地 ASR worker 固定使用扩展内置 ORT WASM 文件，并区分 WebGPU JSEP 与 Tiny CPU/WASM 资源，避免运行时回退到远程资源。
- 模型加载错误保留原始 ORT 错误、`cause`、WASM 路径和 worker 环境诊断，便于定位初始化失败。

### 优化

- 设置页可从 Hugging Face API 刷新三个受支持模型 profile 的安全 revision。
- 修复模型目录默认查询只返回不兼容排序结果，导致刷新后误显示离线 fallback 的问题；旧目录 URL 会自动迁移。
- 网络不可用时回退到内置模型或已缓存的安全 revision；模型下载仍写入用户选择的本地文件夹。
- 增加模型 profile 过滤、路径安全、离线缓存和 Tiny CPU WASM 资源的自动化回归测试。

## 0.9.0 — 2026-08-20

- Debug 页面改为表格列表，每行对应一次翻译请求。
- 支持展开/收起单条记录的完整 JSON 详情，并按类型显示状态徽章。

## 0.8.0 — 2026-08-20

- 重构 Twitch Slate 输入写入为 `insertReplacementText`、`insertText`、剪贴板粘贴三级降级策略。
- 译文写入成功后再发送，降低“输入框未接受译文”的失败率。

## 0.7.0 — 2026-08-19

- 修复重新进入同一直播间后自动翻译只有勾选状态、没有实际监听的问题。
- 修复 Debug 页面抢占 `TRANSLATE` 响应导致聊天翻译间歇性失败的问题。
